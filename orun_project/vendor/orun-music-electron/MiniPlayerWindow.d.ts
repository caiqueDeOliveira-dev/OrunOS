import { BrowserWindow } from 'electron';
/** Opens (or focuses, if already open) a compact always-on-top now-playing window. */
export declare function openMiniPlayer(preloadPath: string, rendererUrl: string): BrowserWindow;
export declare function closeMiniPlayer(): void;
export declare function isMiniPlayerOpen(): boolean;
//# sourceMappingURL=MiniPlayerWindow.d.ts.map